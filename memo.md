그럼 다시 생각해볼게.
엔티티는 우리 LMS 시스템 고유한 식별자가 있고 상태를 저장해야하고 다른 대상관 관계를 맺는 도메인 객체라는거잖아?
1. 무엇을 저장하기 위해 필요
2. 무엇과 무엇의 관계를 저장하기 위해 필요
하니까

크게(현재 1차 mvp 기준)
1. 사용자
2. 시험
3. 문제
로 나눈다.

이 3가지가 상호작용을 하는게 우리 lms 서비스가 운영된다.
그러면 상호작용에 대해 알아보자.

✅ 사용자 - 학생/ 관리자
✅ 시험
 - 시험은 등록->배정->응시->채점 흐름인 Event가 있다.
 - 등록: 관리자가 등록한다
 - 배정: 특정 학생이 어떤 유형의 시험을 풀기 위해선 배정을 해줘야한다.
 - 응시: 특정 유형의 시험들을 배정 받은 학생이 응시할 수 있다.
 - 채점: 채점table을 통해 채점 결과를 저장해줘야한다.
✅ 문제
  - 타입별로 시험에 들어가는 문제들이 다르므로 문제를 저장해야한다.
----

1️⃣ Core Entity(핵심 데이터)
: 시스템에서 "그 자체로 존재하는하는 대상"
- 독립적 존재
- 자체 ID 가짐
- 다른 애들이 참조함
- 서비스가 바뀌어도 유지됨
- DDD 기준 Aggregate Root 가 되는 경우 많음

2️⃣ Relation Enityt(연결 구조)
: 두 Core Entity를 연결하기 위한  엔티티
- FK 2개 이상
- N:M 풀 때 등장
- 추가 속성 가질 수 있음
- 비즈니스 의미를 가지면 별도 엔티티로 승격됨

3️⃣ Action/Event Entity(행위/이력)
: 시간에 따라 발생하는 사건 기록
- 계속 발생(append)
- 상태 변화 추척
- 로그/이력 성격

4️⃣ Derived Entity(계산 결과/ 기준표)
: 계산 결과를 저장하거나 계산 기준을 정의하는 엔티티
- 계산에 사용됨
- 변경 가능성 있음
- 정책 분리 목적
- 조회 성능 최적화 목적
- CORS 구조에서 핵심 개념

====
네트워크 이슈(트러블 이슈) 발생 시
-> 이어풀기 기능

----
로그인 기능

누가 요청했는지 식별하고, 그 사용자가 어떤 API까지 접근한지 서버가 신뢰할 수 있게 만드는 것
====

STEP1 문제의 본질

ERD를 할 때 가장 먼저 가져야 할 관점은 이거다.

“이 서비스는 무엇을 저장해야 돌아가는가?”

코드를 아직 안 짜도 된다.
화면도 아직 안 그려도 된다.
오직 저장해야 하는 상태만 보면 된다.

STEP2 구조 분해

너 프로젝트를 아주 작게 쪼개면 흐름은 이거다.

회원가입
→ 문제 등록
→ 시험 생성
→ 시험 배정
→ 시험 응시
→ 답안 저장
→ 채점
→ 결과 조회

ERD는 결국 이 흐름의 각 지점에서
무슨 데이터가 생기는지를 잡는 작업이다.

STEP3 직관적 설명

ERD 초보가 가장 많이 하는 실수가 있다.

“화면 기준으로 테이블을 만든다”

이러면 망한다.

예를 들어

학생 관리 화면
시험 화면
결과표 화면

이렇게 화면 기준으로 생각하면 안 된다.

반대로 이렇게 생각해야 한다.

학생이라는 데이터가 있나?
시험이라는 데이터가 있나?
학생이 시험을 푼 기록이라는 데이터가 있나?
문제별 답안이라는 데이터가 있나?

즉 화면이 아니라 상태 기준이다.
----
이 질문에 답하면 엔티티 후보가 거의 나온다.

이 시스템에 로그인하는 주체는 누구인가?
학생이 가입할 때 별도로 필요한 인증 수단이 있는가?
시험은 무엇으로 구성되는가?
시험은 그냥 존재하기만 하면 되나, 아니면 학생에게 배정되어야 하나?
학생이 시험을 푸는 “진행 상태”를 저장해야 하나?
점수는 그냥 계산식으로 끝나나, 아니면 별도 기준표가 필요한가?
----


